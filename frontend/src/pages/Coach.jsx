import React from 'react'

export default function Coach() {
  return (
    <div>
      <h2>Coach (Preview)</h2>
      <p>This is a placeholder. Later we can add a coach login and filtered couple overviews (with consent).</p>
    </div>
  )
}
